<?xml version="1.0" encoding="utf-8"?>
<!--
  © 2016 Akretion (Alexis de Lattre <alexis.delattre@akretion.com>)
  License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl).
-->
<odoo>
    <record id="account_payment_order_form" model="ir.ui.view">
        <field name="name">pain.base.account.payment.order.form</field>
        <field name="model">account.payment.order</field>
        <field name="inherit_id" ref="account_payment_order.account_payment_order_form"/>
        <field name="arch" type="xml">
            <field name="company_partner_bank_id" position="after">
                <field invisible="sepa_payment_method == False" name="sepa"/>
                <field invisible="1" name="sepa_payment_method"/>
                <field invisible="1" name="show_warning_not_sepa"/>
                <field name="batch_booking"/>
                <field invisible="sepa == True" name="charge_bearer"/></field>
            <header position="after">
                <div class="alert alert-warning" invisible="show_warning_not_sepa == False" role="alert">
                    This payment order is
                    <b>
                        not SEPA
                    </b>
                    . If it is not intented, check that all payment lines are in € and that all bank accounts are valid IBANs with a country prefix in the
                    <a href="https://en.wikipedia.org/wiki/Single_Euro_Payments_Area" target="_blank">
                        SEPA zone
                    </a>
                    .
                </div>
            </header></field>
    </record>
</odoo>
