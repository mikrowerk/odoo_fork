<?xml version="1.0" encoding="utf-8"?>
<!--
  © 2016 Akretion (Alexis de Lattre <alexis.delattre@akretion.com>)
  License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl).
-->
<odoo>
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account_payment_order.view_move_form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account_payment_partner.view_move_form"/>
        <field name="arch" type="xml">
            <button id="account_invoice_payment_btn" position="after">
                <!-- For customer refunds:
                 'Add to Direct Debit Order' will deduct the refund from a customer invoice
                 We could also need a button 'Add to Payment Order' to reimburse
                 a customer via wire transfer... but I prefer to keep things
                 simple ; to do that, the user should manually create a payment order
                 and select the move lines  -->
                <button groups="account_payment_order.group_account_payment" invisible="payment_order_ok == False or state != 'posted' or move_type not in ('out_invoice', 'out_refund')" name="create_account_payment_line" string="Add to Debit Order" type="object"/>
                <button groups="account_payment_order.group_account_payment" invisible="payment_order_ok == False or state != 'posted' or move_type not in ('in_invoice', 'in_refund')" name="create_account_payment_line" string="Add to Payment Order" type="object"/>
            </button>
            <div name="button_box" position="inside">
                <button groups="account_payment_order.group_account_payment" help="Payment Order Lines" icon="fa-bars" invisible="payment_line_count == 0" name="action_payment_lines" type="object">
                    <field name="payment_line_count" string="Payment Lines" widget="statinfo"/>
                </button>
            </div>
            <field name="payment_mode_id" position="after">
                <field invisible="1" name="payment_order_ok"/></field>
            <field name="payment_reference" position="before">
                <field invisible="move_type not in ('out_invoice', 'out_refund')" name="reference_type" readonly="state != 'draft'" required="move_type in ('out_invoice', 'out_refund')"/></field></field>
    </record>
    <record id="account_invoice_create_account_payment_line_action" model="ir.actions.act_window">
        <field name="name">Add to Payment/Debit Order</field>
        <field name="res_model">account.invoice.payment.line.multi</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="account.model_account_move"/>
    </record>
    <record id="view_invoice_tree" model="ir.ui.view">
        <field name="name">account_payment_order.view_invoice_tree</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <button name="action_register_payment" position="after">
                <button groups="account_payment_order.group_account_payment" invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund')" name="%(account_invoice_create_account_payment_line_action)d" string="Add to Payment/Debit Order" type="action"/>
            </button></field>
    </record>
</odoo>
