<odoo>
    <data>
        <record id="view_job_form" model="ir.ui.view">
            <field name="name">job.input.form</field>
            <field name="model">jobcontrol.job</field>
            <field name="arch" type="xml">
                <form string="Job Input" class="o_sale_order">
                    <!--
                    <header>
                        <button name="create_sales_order" type="object" string="Create Sales Order"
                                class="oe_highlight"/>
                        <field name="state" widget="statusbar" options="{'clickable': '1'}"/>
                    </header>
                    -->

                    <group name="job_header">
                        <group name="job_details">
                            <field name="number"/>
                            <field name="name"/>
                            <field name="category"/>

                            <field name="date_started"/>
                            <field name="date_completed"/>
                            <field name="state"/>

                        </group>
                        <group name="customer_details">
                            <field name="user_id" widget="many2one_avatar_user"/>
                            <field name="partner_company_id"
                                   widget="res_partner_many2one"
                                   context="{'res_partner_search_mode': 'customer', 'show_address': 1}"
                                   placeholder="Type to find a customer..."
                                   readonly="state in ['completed', 'archived']"/>
                            <field name="partner_responsible_id"
                                   widget="res_partner_many2one"
                                   context="{'res_partner_search_mode': 'customer', 'show_address': 1}"
                                   placeholder="Type to find the jobs contact person..."
                                   readonly="state in ['completed', 'archived']"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description">
                            <field name="description" widget="html"/>
                        </page>
                        <page string="Sales Quotations and Orders" name="sales_order_lines">
                            <field name="sales_order_line"
                                   mode="tree">
                                <tree create="True"
                                      string="Sales Order Lines"
                                      delete="False"
                                      limit="100">

                                    <field name="name" string="Number" readonly="1" decoration-bf="1"/>
                                    <field name="client_order_ref" optional="hide"/>
                                    <field name="partner_id" readonly="1"/>
                                    <field name="user_id" widget="many2one_avatar_user" optional="show"/>
                                    <field name="date_order" widget="date" optional="show"
                                           readonly="state in ['cancel', 'sale']"/>
                                    <field name="commitment_date" optional="hide"/>
                                    <field name="expected_date" optional="hide"/>
                                    <field name="validity_date" optional="hide"/>
                                    <field name="currency_id" column_invisible="True"/>
                                    <field name="amount_untaxed"
                                           sum="Total Tax Excluded"
                                           widget="monetary"
                                           optional="hide"/>
                                    <field name="amount_tax"
                                           sum="Tax Total"
                                           widget="monetary"
                                           optional="hide"/>
                                    <field name="amount_total"
                                           sum="Total Tax Included"
                                           widget="monetary"
                                           decoration-bf="1"
                                           decoration-info="invoice_status == 'to invoice'"
                                           optional="show"/>
                                    <field name="amount_to_invoice"
                                           sum="Total Amount to invoice"
                                           widget="monetary"
                                           decoration-bf="1"
                                           decoration-info="invoice_status == 'to invoice'"
                                           optional="hide"/>
                                    <field name="invoice_status"
                                           decoration-success="invoice_status == 'invoiced'"
                                           decoration-info="invoice_status == 'to invoice'"
                                           decoration-warning="invoice_status == 'upselling'"
                                           widget="badge"
                                           optional="show"/>
                                    <field name="tag_ids"
                                           widget="many2many_tags"
                                           options="{'color_field': 'color'}"
                                           optional="hide"/>
                                    <field name="state"
                                           decoration-success="state == 'sale'"
                                           decoration-info="state == 'draft'"
                                           decoration-primary="state == 'sent'"
                                           widget="badge"
                                           optional="show"/>
                                    <field name="message_needaction" column_invisible="True"/>
                                    <button type="object" name="get_formview_action" string="Open" icon="fa-edit"/>
                                </tree>
                            </field>
                        </page>

                        <page string="Deliveries" name="stock_move_lines">
                            <field name="stock_move_lines"
                                   mode="tree"
                                   type="xml">
                                <tree string="Delivery Entries" create="True" delete="False">
                                    <field name="company_id" column_invisible="True"/>
                                    <field name="priority" optional="show" widget="priority" nolabel="1"/>
                                    <field name="name" decoration-bf="1"/>
                                    <field name="location_id" options="{'no_create': True}" string="From"
                                           groups="stock.group_stock_multi_locations" optional="show"
                                           readonly="state == 'done'"/>
                                    <field name="location_dest_id" options="{'no_create': True}" string="To"
                                           groups="stock.group_stock_multi_locations" optional="show"
                                           readonly="state == 'done'"/>
                                    <field name="partner_id" optional="show" readonly="state in ['cancel', 'done']"/>
                                    <field name="is_signed" string="Signed" optional="hide"
                                           groups="stock.group_stock_sign_delivery"/>
                                    <field name="user_id" optional="hide" widget="many2one_avatar_user"
                                           readonly="state in ['cancel', 'done']"/>
                                    <field name="scheduled_date" optional="show" widget="remaining_days"
                                           invisible="state in ('done', 'cancel')"
                                           readonly="state in ['cancel', 'done']"/>
                                    <field name="picking_type_code" column_invisible="True"/>
                                    <field name="products_availability_state" column_invisible="True"
                                           options='{"lazy": true}'/>
                                    <field name="products_availability" options='{"lazy": true}'
                                           optional="hide"
                                           invisible="picking_type_code != 'outgoing' or state not in ['confirmed', 'waiting', 'assigned']"
                                           decoration-success="state == 'assigned' or products_availability_state == 'available'"
                                           decoration-warning="state != 'assigned' and products_availability_state in ('expected', 'available')"
                                           decoration-danger="state != 'assigned' and products_availability_state == 'late'"/>
                                    <field name="date_deadline" optional="hide" widget="remaining_days"
                                           invisible="state in ('done', 'cancel')"/>
                                    <field name="date_done" string="Effective Date" optional="hide"/>
                                    <field name="origin" optional="show" readonly="state in ['cancel', 'done']"/>
                                    <field name="backorder_id" optional="hide"/>
                                    <field name="picking_type_id" optional="hide"/>
                                    <field name="company_id" groups="base.group_multi_company" optional="show"/>
                                    <field name="state" optional="show" widget="badge"
                                           decoration-danger="state=='cancel'"
                                           decoration-info="state== 'assigned'"
                                           decoration-muted="state == 'draft'"
                                           decoration-success="state == 'done'"
                                           decoration-warning="state not in ('draft','cancel','done','assigned')"/>
                                    <field name="activity_exception_decoration" widget="activity_exception"/>
                                    <field name="json_popover" widget="stock_rescheduling_popover" nolabel="1"
                                           invisible="not json_popover"/>
                                    <button type="object" name="get_formview_action" string="Open" icon="fa-edit"/>
                                </tree>
                            </field>
                        </page>

                        <page string="Sales Invoices" name="sales_invoice_entries">
                            <field name="sales_invoice_order_line"
                                   mode="tree"
                                   type="xml">
                                <tree string="Sales Invoice Entries" create="True" delete="False">
                                    <field name="name"/>
                                    <field name="company_currency_id" column_invisible="True"/>
                                    <field name="made_sequence_hole" column_invisible="True"/>
                                    <field name="invoice_date" string="Invoice Date" optional="hide"
                                           readonly="state != 'draft'"/>
                                    <field name="date" readonly="state in ['cancel', 'posted']"/>

                                    <field name="partner_id" optional="show" readonly="state != 'draft'"/>
                                    <field name="ref" optional="hide"/>
                                    <field name="journal_id"/>
                                    <field name="company_id" groups="base.group_multi_company" optional="show"/>
                                    <field name="amount_total_signed" sum="Total Amount" string="Total"
                                           decoration-bf="1"/>
                                    <field name="state" widget="badge" decoration-info="state == 'draft'"
                                           decoration-success="state == 'posted'"/>
                                    <field name="currency_id" column_invisible="True"
                                           readonly="state in ['cancel', 'posted']"/>
                                    <field name="to_check" optional="hide" widget="boolean_toggle"/>
                                    <field name="activity_ids" widget="list_activity" optional="hide"/>
                                    <button type="object" name="get_formview_action" string="Open" icon="fa-edit"/>
                                </tree>
                            </field>
                        </page>

                        <page string="Purchases" name="purchase_lines">
                            <field name="purchase_order_line"
                                   class="o_sale_order"
                                   string="Purchase Order Lines"
                                   limit="100">
                                <tree create="True"
                                      delete="False"
                                      string="Purchase Orders"
                                      limit="100">
                                    <field name="name"/>
                                    <field name="subject"/>
                                    <field name="partner_ref"/>
                                    <field name="partner_id"/>
                                    <field name="user_id" widget="many2one_avatar_user" optional="show"/>
                                    <field name="date_order"/>
                                    <field name="date_planned"
                                           column_invisible="context.get('quotation_only', False)" optional="show"/>
                                    <field name="currency_id" invisible="1" nolabel="1" readonly="1"/>
                                    <field name="amount_untaxed" readonly="1" sum="Total untaxed"
                                           widget="monetary"/>
                                    <field name="amount_total" sum="Total amount" widget="monetary" readonly="1"
                                           optional="show"/>
                                    <field name="state"/>
                                    <button type="object" name="get_formview_action" string="Open" icon="fa-edit"/>
                                </tree>
                            </field>
                        </page>

                        <page string="Purchase Invoices" name="purchase_invoice_lines">
                            <field name="purchase_invoice_order_line"
                                   class="o_sale_order"
                                   limit="100">
                                <tree string="Journal Entries" sample="1" decoration-info="state == 'draft'"
                                      create="True"
                                      delete="False"
                                      expand="context.get('expand', False)">
                                    <field name="name" decoration-danger="made_sequence_hole"/>
                                    <field name="company_currency_id" column_invisible="True"/>
                                    <field name="made_sequence_hole" column_invisible="True"/>
                                    <field name="invoice_date" string="Invoice Date" optional="hide"
                                           readonly="state != 'draft'"/>
                                    <field name="date" readonly="state in ['cancel', 'posted']"/>

                                    <field name="partner_id" optional="show" readonly="state != 'draft'"/>
                                    <field name="ref" optional="hide"/>
                                    <field name="journal_id"/>
                                    <field name="company_id" groups="base.group_multi_company" optional="show"/>
                                    <field name="amount_total_signed" sum="Total Amount" string="Total"
                                           decoration-bf="1"/>
                                    <field name="state" widget="badge" decoration-info="state == 'draft'"
                                           decoration-success="state == 'posted'"/>
                                    <field name="currency_id" column_invisible="True"
                                           readonly="state in ['cancel', 'posted']"/>
                                    <field name="to_check" optional="hide" widget="boolean_toggle"/>
                                    <field name="activity_ids" widget="list_activity" optional="hide"/>
                                    <button type="object" name="get_formview_action" string="Open" icon="fa-edit"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Costs" name="job_costs">
                            <field name="job_cost_line"
                                   class="o_sale_order"
                                   limit="100">
                                <tree create="True">
                                    <field name="name"/>
                                    <field name="product_id"/>
                                    <field name="product_uom_category_id" invisible="1" optional="hide"/>
                                    <field name="product_uom_qty" string="Qty"/>
                                    <field name="product_uom"/>
                                    <field name="currency_id" invisible="1" optional="hide"/>
                                    <field name="not_billable" string="Not billable"/>
                                    <field name="unit_price" string="Unit price" invisible="not_billable"/>
                                    <field name="total_price" string="Total" widget="monetary"
                                           invisible="not_billable"/>
                                    <field name="invoiced" string="invoiced" invisible="not_billable"/>
                                    <field name="description" string="Description" optional="hide"/>
                                    <button type="object" name="get_formview_action" string="Open" icon="fa-edit"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Events" name="events_for_job" invisible="category != 'EVENT'">
                            <field name="category" invisible="True"/>
                            <field name="job_event_line"
                                   class="o_sale_order"
                                   limit="100">
                                <tree create="True" delete="False">
                                    <field name="name"/>
                                    <field name="start_date"/>
                                    <field name="end_date"/>
                                    <field name="description" string="Description" optional="hide"/>
                                    <button type="object" name="get_formview_action" string="Open" icon="fa-edit"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Task Management">
                            <field name="task_management_line"
                                   class="o_sale_order"
                                   limit="100">
                                <tree decoration-muted="active == False" string="Task Management" create="True"
                                      delete="False" default_order="is_favorite desc, sequence, name, id"
                                      js_class="project_project_list">
                                    <field name="sequence" column_invisible="True"/>
                                    <field name="name" column_invisible="1"/>
                                    <field name="message_needaction" column_invisible="True"/>
                                    <field name="active" column_invisible="True"/>
                                    <field name="is_favorite" string="Favorite" nolabel="1" widget="boolean_favorite"
                                           optional="hide"/>
                                    <field name="display_name" string="Name" class="fw-bold"/>
                                    <field name="date_start" string="Planned Date" widget="daterange"
                                           options="{'end_date_field': 'date', 'always_range': '1'}" optional="hide"/>
                                    <field name="date" column_invisible="True"/>
                                    <field name="user_id" optional="show" string="Project Manager"
                                           widget="many2one_avatar_user"
                                           options="{'no_open':True, 'no_create': True, 'no_create_edit': True}"/>
                                    <field name="last_update_color" column_invisible="True"/>
                                    <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"
                                           optional="show"/>
                                    <field name="last_update_status" string="Status" optional="show"
                                           widget="project_state_selection"/>
                                    <field name="company_id" column_invisible="True"/>
                                    <field name="stage_id" options="{'no_open': True}"
                                           domain="[('company_id', 'in', (company_id, False))]" optional="show"/>
                                    <button string="View Tasks" name="action_view_tasks" type="object"/>
                                    <button type="object" name="get_formview_action" string="Open" icon="fa-edit"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                    <div class="oe_chatter">
                        <field name="message_follower_ids"/>
                        <field name="activity_ids"/>
                        <field name="message_ids"/>
                    </div>
                </form>
            </field>
        </record>


        <!-- explicit job list view definition -->
        <record model="ir.ui.view" id="jobcontrol.list">
            <field name="name">jobcontrol list</field>
            <field name="model">jobcontrol.job</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="number"/>
                    <field name="name"/>
                    <field name="category"/>
                    <field name="partner_company_id" optional="show"/>
                    <!--                    <field name="partner_responsible_id"/>-->
                    <field name="user_id" widget="many2one_avatar_user"/>
                    <field name="date_started"/>
                    <field name="date_completed"/>
                    <field name="state"
                           decoration-success="state == 'completed'"
                           decoration-info="state == 'ordered' or state == 'progress'"
                           decoration-primary="state == 'invoiced'"
                           decoration-danger="state == 'danger'"
                           decoration-muted="state == 'archived'"
                           nolabel="1"
                           widget="badge"/>
                </tree>
            </field>
        </record>
    </data>
</odoo>
